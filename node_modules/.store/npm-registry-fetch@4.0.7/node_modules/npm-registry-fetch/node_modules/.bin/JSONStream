#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="$basedir/D:/zfonlyone/node_modules/.store/JSONStream@1.3.5/node_modules"
else
  export NODE_PATH="$NODE_PATH:$basedir/D:/zfonlyone/node_modules/.store/JSONStream@1.3.5/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../../JSONStream@1.3.5/node_modules/JSONStream/bin.js" "$@"
else
  exec node  "$basedir/../../../../../JSONStream@1.3.5/node_modules/JSONStream/bin.js" "$@"
fi
